pre-commit:
  parallel: true
  jobs:
    - name: just format
      glob: "justfile"
      run: just --fmt
      stage_fixed: true
    - name: css-build
      run: just css
      stage_fixed: true
    - name: keep-sorted
      run: keep-sorted {staged_files}
      stage_fixed: true
